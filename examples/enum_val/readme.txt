Examples of values replaced by enum names, including one where the enum definition is an array of names and one where it's an object of value/name pairs.

Run with:
python3 bin_to_c.py examples/enum_val/data.bin examples/enum_val/input.json
